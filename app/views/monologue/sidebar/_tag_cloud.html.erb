<%= sidebar_section_for('Tags') do  %>
  <% @tags.each do |tag| %>
    <%=link_to tag.name, tag_url(tag), :class =>"label-size-#{size_for_tag(tag,@tags_frequency_min,@tags_frequency_max)}" %>
  <% end %>
<% end  %>