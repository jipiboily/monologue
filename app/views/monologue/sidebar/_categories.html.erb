<%= sidebar_section_for('Categories') do %>
   <ul>
     <% @tags.each do |tag| %>
       <li id="tag"><%=link_to "#{tag.name} (#{tag.frequency})", tag_url(tag) %> </li>
     <% end %>
   </ul>
 <% end %>